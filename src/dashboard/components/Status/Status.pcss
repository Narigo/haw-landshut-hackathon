.status {
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  align-items: center;
  overflow: hidden;
  height: 4em;
  padding: 0.5em 1em;

  & .text {
    color: var(--overlayTextColor);
    font-size: 2em;
  }
}
